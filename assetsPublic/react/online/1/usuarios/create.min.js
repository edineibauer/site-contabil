/*
if(dados.token_recovery !== "" && !isNaN(dados.token_recovery) && dados.token_recovery > 0) {
    if(dados.setor === 2) {
        //vincula contador
        dbLocal.exeRead("contador", dados.token_recovery).then(d => {
            d.usuario_id = dados.id;
            db.exeCreate("contador", d);
        });

    } else if(dados.setor === 3) {
        //vincula cliente
        dbLocal.exeRead("clientes", dados.token_recovery).then(d => {
            d.usuario_id = dados.id;
            db.exeCreate("clientes", d);
        });
    }
}*/
